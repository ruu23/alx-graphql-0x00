# Episode Query and Character Pagination

This folder contains:
1. A GraphQL query to fetch a specific episode using `episode(id: ID!)`.
2. Four GraphQL queries to fetch paginated characters using `characters(page: Int)`.

## Episode Query Fields:
- id
- name
- air_date
- episode

## Characters Query Fields:
- id
- name
- status
- image
